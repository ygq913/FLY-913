# BHuman 2025 代码修改记录（完整版）

> 基于 BHuman 官方 Code Release 版本，针对国内 5v5 比赛规则和实际需求所做的全部修改。
> 对比基准：`BHumanCodeRelease-master/`（官方原版）
> 生成日期：2026年4月20日

---

## 修改总览

| 类别 | 修改文件数 | 核心目的 |
|------|-----------|----------|
| 5v5 策略适配 | ~10 | 适配国内 5v5 比赛赛制 |
| 开场站位 | 3 | 2+3 不对称站位分布 |
| 口哨检测 | 2 | 降低阈值，不依赖 GC 独立检测 |
| 裁判手势识别 | 2 | 降低阈值，扩大识别范围和角度 |
| 通信优化 | 1 | 更短触发间隔，更快响应 |
| 开球/传球策略 | 5 | 取消间接开球限制，己方半场传球限制 |
| SimRobot 扩展 | 8 | 外部 GC3、kiba 命令、TCP 控制 |
| 日志回放 | 1 | 自动推进到 playing 状态 |
| 机器人/队伍配置 | 2+15 目录 | 注册实体机器人，配置队伍 |
| 其他 | ~8 | 测试场景、绘图、IP 预设等 |

---

## 一、5v5 策略适配（国内比赛）

国内比赛采用 5 台机器人 vs 5 台机器人的赛制，而 BHuman 官方默认为 7v7。

### 1.1 策略切换

| 文件 | 修改内容 |
|------|----------|
| `Config/Scenarios/Default/strategyBehaviorControl.cfg` | `strategy = attacking7v7` → `strategy = s5v5` |
| `Config/settings.cfg` | `scenario = Default` → `scenario = 5vs5` |

### 1.2 新增/修改 5v5 策略文件

| 文件 | 说明 |
|------|------|
| `Config/Behavior/Strategies/s5v5.cfg` | **新增/修改**，定义 5v5 比赛用战术转换 |
| `Config/Scenarios/5vs5/setupPosesProvider.cfg` | 5v5 场景站位配置 |
| `Config/Scenarios/5vs5/strategyBehaviorControl.cfg` | 5v5 场景策略配置 |

### 1.3 SimRobot 仿真场景从 7vs7 改为 5vs5

| 文件 | 修改内容 |
|------|----------|
| `Config/Scenes/Game.con` | `call Includes/7vs7` → `call Includes/5vs5` |
| `Config/Scenes/GameFast.con` | 同上 |
| `Config/Scenes/GamePerceptOracle.con` | 同上 |
| `Config/Scenes/Game.ros3` | `Includes/7vs7.rsi3` → `Includes/5vs5.rsi3` |
| `Config/Scenes/GamePerceptOracle.ros3` | 同上 |
| `Config/Scenes/OtherScenes/OneTeamReferee.ros3` | `7vsDummies.rsi3` → `5vs5.rsi3` |
| `Config/Scenes/Includes/5vs5.rsi3` | **新增/修改**，5v5 场景的机器人布局定义 |

---

## 二、开场站位修改（一边 2 个，一边 3 台）

### 2.1 Default 场景站位调整

**文件**：`Config/Scenarios/Default/setupPosesProvider.cfg`

原版为左右对称各 4 个站位（1-4 左侧，5-7 右侧），共 7+1 个位置。修改后：

- 去掉了 6、7 号位，改为左侧 4 个、右侧 2 个共 6 个站位
- 各站位坐标调整（更往后退）

| 球员号 | 原坐标 | 新坐标 |
|--------|--------|--------|
| 1 | (-3900, 3000) | (-4100, 3000) |
| 2 | (-2850, 3000) | (-3100, -3000) **换侧** |
| 3 | (-1800, 3000) | (-2200, 3000) |
| 4 | (-750, 3000) | (-1100, 3000) |
| 5 | (-750, -3000) | (-1000, -3000) |
| 99 | (-3900, -3000) | (-4100, -3000) |
| ~~6~~ | ~~(-1800, -3000)~~ | 已删除 |
| ~~7~~ | ~~(-2850, -3000)~~ | 已删除 |

### 2.2 5vs5 场景站位

**文件**：`Config/Scenarios/5vs5/setupPosesProvider.cfg`

定义 5v5 专用站位：左侧 3 个 + 右侧 2 个 + 守门员位。

### 2.3 SetupPosesProvider 代码修改

**文件**：`Src/Modules/Configuration/SetupPosesProvider/SetupPosesProvider.cpp`

- 新增 `#include "Framework/Settings.h"` 和 `#include <algorithm>`
- 球员编号改为使用 `Settings::lowestValidPlayerNumber` 作为起始编号
- 添加对 `currentRobotOrder` 的排序，确保编号顺序一致

---

## 三、口哨检测阈值降低

### 3.1 口哨灵敏度参数

**文件**：`Config/Scenarios/Default/gameStateProvider.cfg`

| 参数 | 原值 | 新值 | 说明 |
|------|------|------|------|
| `minVotersForWhistle` | 5 | **1** | 只需 1 台机器人检测到口哨即可触发 |
| `minWhistleAverageConfidence` | 1.15 | **0.5** | 口哨置信度阈值大幅降低 |

### 3.2 口哨检测逻辑修改（不依赖 GameController）

**文件**：`Src/Modules/Infrastructure/GameStateProvider/GameStateProvider.cpp`

- 将 `waitFor*` 状态下的口哨检测从 `gameControllerActive` 的 switch-case 分支中**提取出来**
- 改为**独立的 switch 块**，始终检测口哨，不再受 GameController 是否激活的限制
- 目的：适配中国比赛规则，允许口哨和 GameController 同时触发状态转换

---

## 四、裁判手势识别优化

### 4.1 裁判手势分类器参数

**文件**：`Config/Scenarios/Default/refereeGestureClassifier.cfg`

| 参数 | 原值 | 新值 | 说明 |
|------|------|------|------|
| `refereeHeight` | 2400 | **2000** | 裁判身高估计降低（适配国内裁判） |
| `netThreshold` | 0.9 | **0.6** | 神经网络置信度阈值大幅降低 |

### 4.2 HandleRefereeSignal 参数调整

**文件**：`Src/Modules/BehaviorControl/SkillBehaviorControl/Options/HandleRefereeSignal.cpp`

| 参数 | 原值 | 新值 | 说明 |
|------|------|------|------|
| `upperImageBorderAtHeight` | 2400mm | **2800mm** | 抬头看更高位置 |
| `upperImageBorderAtHeightStandby` | 2600mm | **2400mm** | 待机时看的高度 |
| `distanceRange` | {2000, 6000} | **{1200, 9000}** | 更远距离也可识别裁判 |
| `bearingRange` | {60°, 120°} | **{10°, 170°}** | 几乎任何角度都可激活 |
| `kickInWaitTime` | 6000ms | **15000ms** | 等待识别时间从 6 秒增加到 15 秒 |
| `maxHeadTurn` | 45° | **70°** | 头部可转更大角度找裁判 |

---

## 五、通信参数优化（更快响应、更短触发间隔）

**文件**：`Config/Scenarios/Default/teamMessageHandler.cfg`

| 参数 | 原值 | 新值 | 说明 |
|------|------|------|------|
| `normalMessageReserve` | 30 | **40** | 更保守的消息预留 |
| `positionThreshold` | 300mm | **280mm** | 更小的位置变化就触发通信 |
| `teamBallThresholdBase` | 9000ms | **7500ms** | 团队球过期时间缩短 |
| `teamBallThresholdFactor` | 500 | **400** | 每球员号增加时间降低 |
| `disappearedThreshold` | 3000ms | **2500ms** | 球消失判定更快 |
| `minTimeBallIsNotNearTeamBall` | 300ms | **250ms** | 球位置差异持续时间缩短 |
| `sendDelayRange` | {300, 1200} | **{250, 1100}** | 发送延迟范围降低 |

---

## 六、开球策略修改

**文件**：`Src/Modules/BehaviorControl/SkillBehaviorControl/Skills/Ball/DirectKickOff.cpp`

注释掉了间接开球时的障碍物扇区限制：

```cpp
// if(!theIndirectKick.allowDirectKick)
// {
//   obstacleSectors.emplace_back();
//   obstacleSectors.back().sector = Rangea(-35_deg, 35_deg);
//   obstacleSectors.back().distance = theFieldDimensions.centerCircleRadius;
//   obstacleSectors.back().x = theFieldDimensions.centerCircleRadius;
// }
```

**效果**：开球时不再限制直接踢球方向。

---

## 七、传球限制优化

### 7.1 己方半场传球限制配置

以下三个文件均新增相同参数：

| 文件 | 新增参数 |
|------|----------|
| `Config/Scenarios/Default/optionPassToTeammate.cfg` | `restrictPassingInOwnHalf = true; minForwardDistanceInOwnHalf = 200` |
| `Config/Scenarios/Default/K1/optionPassToTeammate.cfg` | 同上 |
| `Config/Scenarios/Default/T1/optionPassToTeammate.cfg` | 同上 |

### 7.2 PassToTeammate.cpp 修改

**文件**：`Src/Modules/BehaviorControl/SkillBehaviorControl/Skills/Ball/PassToTeammate.cpp`

**新增参数定义**：
```cpp
(bool) restrictPassingInOwnHalf,  /**< 在己方半场时，只传给比球更靠近对方球门的队友 */
(float) minForwardDistanceInOwnHalf), /**< 队友必须比球至少靠前多少毫米 */
```

**核心逻辑**（约第 112-131 行）：
```cpp
// 检查是否在己方半场且需要应用限制
const Vector2f& ballPositionOnField = theFieldInterceptBall.interceptedEndPositionOnField;
const bool ballInOwnHalf = ballPositionOnField.x() < theFieldDimensions.xPosHalfwayLine;
const bool applyOwnHalfRestrictions = restrictPassingInOwnHalf && ballInOwnHalf;

if(applyOwnHalfRestrictions)
{
  // 只传给比球更靠近对方球门的队友
  const float teammateForwardDistance = t.pose.translation.x() - ballPositionOnField.x();
  if(teammateForwardDistance < minForwardDistanceInOwnHalf)
  {
    // 队友不够靠前，跳过这个传球目标
    ANNOTATION("PassToTeammate", "Teammate " << playerNumber << " rejected: too far back in own half...");
    break;
  }
}
```

**效果**：

| 场景 | 原版行为 | 修改后行为 |
|------|----------|-----------|
| 球在己方半场 | 可以传给任何队友 | 只能传给**比球更靠近对方球门**的队友，且距离差至少 **200mm** |
| 球在对方半场 | 无限制 | 无限制 |

**目的**：防止在己方危险区域往回传球，减少被对手抢断后直接面对空门的风险。

---

## 八、SimRobot 工具扩展

### 8.1 新增 CommandServer（TCP 外部控制）

| 文件 | 说明 |
|------|------|
| `Src/Libs/SimulatedNao/CommandServer.h` | **新增文件**，TCP 命令服务器头文件 |
| `Src/Libs/SimulatedNao/CommandServer.cpp` | **新增文件**，监听 TCP 12345 端口，接受外部 GUI 控制 |

### 8.2 外部 GameController3 支持

**文件**：`Src/Libs/SimulatedNao/GameController.h` / `GameController.cpp`

- 新增 UDP 收发 socket（端口 3838 接收 GC 消息，端口 3939 发送机器人状态）
- 新增方法：`enableExternalGameController()`、`processExternalGameController()`、`sendRobotStatusToGC()`
- 通过 `gc external on|off` 命令控制

### 8.3 新增 `kiba` 命令（踢球到目标点）

| 文件 | 修改内容 |
|------|----------|
| `Src/Libs/SimulatedNao/RobotConsole.cpp` | 新增 `kiba <x> <y> <speed>` 命令处理 |
| `Src/Libs/SimulatedNao/RobotConsole.h` | 新增 `kickBall()` 方法声明 |
| `Src/Libs/SimulatedNao/SimulatedRobot.cpp` | 新增 `setBallVelocity()` 和 `kickBallPerTeam()` 实现 |
| `Src/Libs/SimulatedNao/SimulatedRobot.h` | 新增对应方法声明 |

### 8.4 ConsoleRoboCupCtrl 集成

**文件**：`Src/Libs/SimulatedNao/ConsoleRoboCupCtrl.cpp` / `.h`

- 启动时创建 CommandServer
- 新增 `gc external on|off` 命令
- 主循环中处理外部命令

### 8.5 新增 SimulatedNao11 目录

`Src/Libs/SimulatedNao11/`：**新增目录**（11 人制仿真相关）

---

## 九、日志回放修改

**文件**：`Config/Scenes/ReplayRobot.con`

- 原版：手动关闭 Cognition 的 GameControllerData / GameState
- 修改后：让日志自动配置，并依次执行 `gc standby` → `gc ready` → `gc set` → `gc playing`，自动推进到 playing 状态以激活自定位

---

## 十、机器人与队伍配置

### 10.1 机器人注册

**文件**：`Config/Robots/robots.cfg`

新增 15 台机器人注册信息（含 head/body ID）：

| 机器人名 | 类型 |
|----------|------|
| fly1, fly3, fly4, fly5, fly6, fly7, fly8 | Nao |
| fly11, fly12, fly13, fly14, fly15 | Nao |
| nb21, nb22, nb23 | Nao |

新增对应的机器人配置目录：
- `Config/Robots/fly1/`
- `Config/Robots/fly3/`
- `Config/Robots/fly4/`
- `Config/Robots/fly5/`
- `Config/Robots/fly6/`
- `Config/Robots/fly7/`
- `Config/Robots/fly8/`
- `Config/Robots/fly11/`
- `Config/Robots/fly12/`
- `Config/Robots/fly13/`
- `Config/Robots/fly14/`
- `Config/Robots/fly15/`
- `Config/Robots/nb21/`
- `Config/Robots/nb22/`
- `Config/Robots/nb23/`

### 10.2 队伍配置

**文件**：`Config/teams.cfg`

| 配置项 | 原值 | 新值 |
|--------|------|------|
| 队名 | "B-Human Code Release" | "B-Human" |
| 队号 | 89 | **5** |
| 场上球员颜色 | blue | **black** |
| 守门员颜色 | red | **purple** |
| WiFi | NONE | **SPL_A** |
| 音量 | 100 | **63** |
| 球员分配 | 无 | fly15, fly1 等 |

新增第二支队伍：
- 队名：**B-Team**，队号：**70**
- 颜色：red / blue
- 球员分配：fly12 等

---

## 十一、守门员速度滤波（已回退）

⚠️ **此部分修改已回退到原版，当前代码与官方 B-Human 一致**

### 11.1 曾修改但已回退的文件

以下文件曾被修改添加 SPQR 风格的速度威胁判定，但现已恢复为原版：

- `Src/Representations/BehaviorControl/FieldInterceptBall.h`
- `Src/Modules/BehaviorControl/FieldBallProvider/FieldInterceptBallProvider.h`
- `Src/Modules/BehaviorControl/FieldBallProvider/FieldInterceptBallProvider.cpp`
- `Src/Modules/BehaviorControl/SkillBehaviorControl/Skills/Ball/InterceptBall.cpp`
- `Config/Scenarios/Default/fieldInterceptBallProvider.cfg`

### 11.2 与守门员相关的其他文件

以下文件与原版一致，未做修改：

- `Src/Modules/BehaviorControl/StrategyBehaviorControl/PositionRoles/Goalkeeper.cpp`
- `Src/Modules/BehaviorControl/StrategyBehaviorControl/PositionRoles/Goalkeeper.h`

### 11.3 仍需注意的残余改动

以下文件因支持日志回放功能，仍与原版有差异（添加了对 FieldInterceptBall 的支持）：

- `Src/Libs/Streaming/MessageIDs.h`
- `Src/Modules/Infrastructure/LogDataProvider/LogDataProvider.h`

---

## 十二、其他修改

| 文件 | 修改内容 |
|------|----------|
| `Config/Scenes/Drawings/Tactics.con` | 新增守门员相关战术绘图（`Goalkeeper:pose`、`Goalkeeper:coverage`） |
| `Config/Scenes/Includes/connectDialog.con` | 预设所有机器人 IP 地址（192.168.70.x 和 10.0.70.x 网段） |
| `Src/Modules/BehaviorControl/PathPlannerProvider/PathPlannerProvider.cpp` | 注释掉 `theValidityDuck.wav` 鸭子叫声 |

### 12.1 libDemoProvider.cfg 位置调整

- **原版位置**：`Config/Scenarios/Default/libDemoProvider.cfg`
- **当前位置**：`Config/Scenarios/libDemoProvider.cfg`（移到上层目录）

**当前配置内容**：
```ini
isDemoActive = false;
isOneVsOneDemoActive = true;
isMiniFieldActive = false;
activeDemoGameStates = [soccer];
changeArmToWave = false;
```

**说明**：演示模式当前未启用（`isDemoActive = false`）。

### 12.2 新增测试/调试场景

| 文件 | 说明 |
|------|------|
| `Config/Scenes/Game1v1.con` / `Game1v1.ros3` | 1v1 测试场景 |
| `Config/Scenes/KeeperTest.con` / `KeeperTest.ros3` | 守门员测试场景 |
| `Config/Scenes/Includes/1v1_GoalkeeperVsForward.con` / `.rsi3` | 1v1 守门员对前锋场景 |
| `Config/Scenes/OtherScenes/OneTeamReferee11.ros3` | 11 人制裁判场景 |

---

## 附：与原版的主要差异清单

### 修改的文件（Modified）

**Config 目录：**
- `Config/Robots/Default/odometryDataPreviewProvider.cfg`
- `Config/Robots/robots.cfg`
- `Config/Scenarios/Default/K1/optionPassToTeammate.cfg`
- `Config/Scenarios/Default/T1/optionPassToTeammate.cfg`
- `Config/Scenarios/Default/fieldInterceptBallProvider.cfg`（已回退到原版）
- `Config/Scenarios/Default/gameStateProvider.cfg`
- `Config/Scenarios/Default/optionPassToTeammate.cfg`
- `Config/Scenarios/Default/refereeGestureClassifier.cfg`
- `Config/Scenarios/Default/setupPosesProvider.cfg`
- `Config/Scenarios/Default/strategyBehaviorControl.cfg`
- `Config/Scenarios/Default/teamMessageHandler.cfg`
- `Config/Scenes/Drawings/Tactics.con`
- `Config/Scenes/Game.con`
- `Config/Scenes/Game.ros3`
- `Config/Scenes/GameFast.con`
- `Config/Scenes/GameFast.ros3`
- `Config/Scenes/GamePerceptOracle.con`
- `Config/Scenes/GamePerceptOracle.ros3`
- `Config/Scenes/Includes/connectDialog.con`
- `Config/Scenes/OtherScenes/OneTeamReferee.ros3`
- `Config/Scenes/ReplayRobot.con`
- `Config/settings.cfg`
- `Config/teams.cfg`

**Src 目录：**
- `Src/Libs/SimulatedNao/ConsoleRoboCupCtrl.cpp`
- `Src/Libs/SimulatedNao/ConsoleRoboCupCtrl.h`
- `Src/Libs/SimulatedNao/GameController.cpp`
- `Src/Libs/SimulatedNao/GameController.h`
- `Src/Libs/SimulatedNao/RobotConsole.cpp`
- `Src/Libs/SimulatedNao/RobotConsole.h`
- `Src/Libs/SimulatedNao/SimulatedRobot.cpp`
- `Src/Libs/SimulatedNao/SimulatedRobot.h`
- `Src/Libs/Streaming/MessageIDs.h`
- `Src/Modules/BehaviorControl/FieldBallProvider/FieldInterceptBallProvider.cpp`（已回退到原版）
- `Src/Modules/BehaviorControl/PathPlannerProvider/PathPlannerProvider.cpp`
- `Src/Modules/BehaviorControl/SkillBehaviorControl/Options/HandleRefereeSignal.cpp`
- `Src/Modules/BehaviorControl/SkillBehaviorControl/Skills/Ball/DirectKickOff.cpp`
- `Src/Modules/BehaviorControl/SkillBehaviorControl/Skills/Ball/InterceptBall.cpp`（已回退到原版）
- `Src/Modules/BehaviorControl/SkillBehaviorControl/Skills/Ball/PassToTeammate.cpp`
- `Src/Modules/Configuration/SetupPosesProvider/SetupPosesProvider.cpp`
- `Src/Modules/Infrastructure/GameStateProvider/GameStateProvider.cpp`
- `Src/Modules/Infrastructure/LogDataProvider/LogDataProvider.h`

### 新增的文件（Added）

**Src 目录：**
- `Src/Libs/SimulatedNao/CommandServer.cpp`
- `Src/Libs/SimulatedNao/CommandServer.h`

**Config 目录：**
- `Config/Scenarios/libDemoProvider.cfg`（从 Default 目录移至上层）
- `Config/Behavior/Strategies/s5v5.cfg`

**新增目录：**
- `Src/Libs/SimulatedNao11/`
- `Config/Robots/fly1/`、`fly3/`、`fly4/`、`fly5/`、`fly6/`、`fly7/`、`fly8/`
- `Config/Robots/fly11/`、`fly12/`、`fly13/`、`fly14/`、`fly15/`
- `Config/Robots/nb21/`、`nb22/`、`nb23/`

---

## 修改验证

### 守门员部分一致性确认

以下文件已与原版 B-Human 逐文件对比确认一致：

✅ `Src/Modules/BehaviorControl/StrategyBehaviorControl/PositionRoles/Goalkeeper.cpp`
✅ `Src/Modules/BehaviorControl/StrategyBehaviorControl/PositionRoles/Goalkeeper.h`
✅ `Src/Modules/BehaviorControl/SkillBehaviorControl/Skills/Ball/InterceptBall.cpp`
✅ `Src/Modules/BehaviorControl/FieldBallProvider/FieldInterceptBallProvider.cpp`
✅ `Src/Modules/BehaviorControl/FieldBallProvider/FieldInterceptBallProvider.h`
✅ `Src/Representations/BehaviorControl/FieldInterceptBall.h`

**结论**：守门员相关代码已与原版 B-Human 框架完全一致。

---

*文档结束*
