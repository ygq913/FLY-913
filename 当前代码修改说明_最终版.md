# BHuman 当前代码修改说明（最终落地版）

> 说明：
> - 本文档用于记录当前代码库中**已经实际落地**的修改内容。
> - 对比基准为官方 `BHuman Code Release` 原版。
> - 本文档特别区分了：**已生效修改**、**曾计划/曾改过但当前已回退**、**文档里提到但当前未落地**。
> - 也包含本次为使 `SimulatedNao` 成功编译而做的兼容性修复。

---

# 一、总体结论

当前代码库的修改主要集中在以下几个方向：

- 适配国内 **5v5** 比赛
- 调整 **开场站位**
- 优化 **口哨检测**
- 优化 **裁判手势识别**
- 调整 **通信参数**
- 扩展 **SimRobot / SimulatedNao 工具链**
- 新增 **外部 GameController3 支持**
- 新增 **TCP 命令控制**
- 新增 **kiba 踢球命令**
- 更新 **队伍与机器人配置**

同时，需要特别说明：

- 文档中提到的 **第六点“开球策略修改”**，当前代码中**没有生效**，已经保持原版逻辑。
- 文档中提到的 **第七点“己方半场传球限制”**，当前代码中**没有生效**，已经保持原版逻辑。
- 守门员相关的部分，当前整体仍以**原版 BHuman 行为**为主。

---

# 二、已生效的修改内容

## 1. 5v5 策略适配

### 1.1 默认场景切换到 5v5

已修改内容：

- `Config/settings.cfg`
- `Config/Scenarios/Default/strategyBehaviorControl.cfg`

主要效果：

- 默认场景从原版设置切换为 `5vs5`
- 默认策略从原版的 7v7 逻辑切换为 5v5 对应策略 `s5v5`

### 1.2 新增/调整 5v5 对应配置

涉及内容：

- `Config/Behavior/Strategies/s5v5.cfg`
- `Config/Scenarios/5vs5/setupPosesProvider.cfg`
- `Config/Scenarios/5vs5/strategyBehaviorControl.cfg`

主要作用：

- 为 5v5 提供单独的策略与站位配置
- 使系统在 5 台机器人比赛环境下使用更合适的行为配置

### 1.3 仿真场景改为 5v5

涉及内容：

- `Config/Scenes/Game.con`
- `Config/Scenes/GameFast.con`
- `Config/Scenes/GamePerceptOracle.con`
- `Config/Scenes/Game.ros3`
- `Config/Scenes/GamePerceptOracle.ros3`
- `Config/Scenes/OtherScenes/OneTeamReferee.ros3`
- `Config/Scenes/Includes/5vs5.rsi3`

主要作用：

- SimRobot 中默认比赛场景由 7v7 调整为 5v5
- 使仿真与当前国内比赛赛制保持一致

---

## 2. 开场站位修改

### 2.1 Default 场景站位修改

涉及文件：

- `Config/Scenarios/Default/setupPosesProvider.cfg`

主要修改方向：

- 原版站位布局进行了调整
- 由原来的 7v7 默认布局，改为适合当前使用方式的较少人数站位
- 站位整体更偏保守、后撤

### 2.2 5vs5 站位配置

涉及文件：

- `Config/Scenarios/5vs5/setupPosesProvider.cfg`

主要作用：

- 为 5v5 单独配置开场站位
- 适应 5 台机器人时的初始分布

### 2.3 SetupPosesProvider 代码修改

涉及文件：

- `Src/Modules/Configuration/SetupPosesProvider/SetupPosesProvider.cpp`

主要修改：

- 增加对 `Settings` 的使用
- 使用 `Settings::lowestValidPlayerNumber` 作为球员编号起始点
- 对 `currentRobotOrder` 做排序，保证编号顺序一致

主要作用：

- 保证开场站位映射更稳定
- 在机器人数量变化时减少编号错乱风险

---

## 3. 口哨检测优化

### 3.1 参数调整

涉及文件：

- `Config/Scenarios/Default/gameStateProvider.cfg`

主要修改：

- `minVotersForWhistle` 降低
- `minWhistleAverageConfidence` 降低

主要作用：

- 提高口哨触发灵敏度
- 更适配国内比赛环境中的口哨识别需求

### 3.2 逻辑调整

涉及文件：

- `Src/Modules/Infrastructure/GameStateProvider/GameStateProvider.cpp`

主要修改：

- 将口哨检测逻辑从依赖 `gameControllerActive` 的流程中分离出来
- 使口哨检测在相关状态下可以独立工作

主要作用：

- 降低对外部 GC 状态的依赖
- 允许口哨检测在更多实际比赛场景下发挥作用

---

## 4. 裁判手势识别优化

### 4.1 分类器参数调整

涉及文件：

- `Config/Scenarios/Default/refereeGestureClassifier.cfg`

主要修改：

- 降低 `refereeHeight`
- 降低 `netThreshold`

主要作用：

- 放宽裁判识别条件
- 更适配实际比赛中裁判距离、姿态、身高差异

### 4.2 行为控制参数调整

涉及文件：

- `Src/Modules/BehaviorControl/SkillBehaviorControl/Options/HandleRefereeSignal.cpp`

主要修改方向：

- 调整抬头看的高度
- 扩大识别距离范围
- 扩大 bearing 范围
- 延长 `kickInWaitTime`
- 增大头部最大转动范围

主要作用：

- 提高裁判手势识别触发概率
- 扩大机器人寻找裁判的有效空间范围

---

## 5. 通信参数优化

涉及文件：

- `Config/Scenarios/Default/teamMessageHandler.cfg`

主要修改方向：

- 调整消息预留量
- 降低位置变化触发阈值
- 缩短 team ball 相关超时阈值
- 缩短消失判定时间
- 缩短发送延迟范围

主要作用：

- 让队内通信更新更积极
- 让队友状态、球状态更新更及时
- 改善比赛时的信息响应速度

---

## 6. 传球相关已生效优化

### 6.1 lookAhead 已启用

涉及文件：

- `Config/Scenarios/Default/optionPassToTeammate.cfg`
- `Config/Scenarios/Default/K1/optionPassToTeammate.cfg`
- `Config/Scenarios/Default/T1/optionPassToTeammate.cfg`

当前已确认生效的内容：

- `lookAhead = true`
- `passAheadDistance = 0`

主要作用：

- 传球时考虑接球队友未来位置
- 减少把球传到队友“旧位置”的情况
- 在不额外激进改策略的情况下提升传球质量

---

## 7. SimRobot / SimulatedNao 扩展

这是当前代码中改动较大、也已经编译验证通过的一部分。

### 7.1 新增 TCP 命令服务器

新增文件：

- `Src/Libs/SimulatedNao/CommandServer.h`
- `Src/Libs/SimulatedNao/CommandServer.cpp`

主要功能：

- 启动 TCP 服务端
- 监听外部命令连接
- 接收外部 GUI 或脚本发送的文本命令

主要用途：

- 便于外部程序控制 SimRobot / SimulatedNao
- 为后续联动调试工具提供接口

### 7.2 外部 GameController3 支持

涉及文件：

- `Src/Libs/SimulatedNao/GameController.h`
- `Src/Libs/SimulatedNao/GameController.cpp`

主要新增能力：

- 支持外部 UDP GameController 消息接入
- 支持从外部 GC 接收比赛控制消息
- 支持向外部 GC 发送机器人状态
- 使用端口收发外部 GC 数据

新增/修改的核心接口：

- `enableExternalGameController()`
- `processExternalGameController()`
- `sendRobotStatusToGC()`

主要作用：

- 使 SimRobot 可以与外部 GameController3 对接
- 有利于与真实比赛控制流程做一致性测试

### 7.3 新增 `kiba` 命令

涉及文件：

- `Src/Libs/SimulatedNao/RobotConsole.cpp`
- `Src/Libs/SimulatedNao/RobotConsole.h`
- `Src/Libs/SimulatedNao/SimulatedRobot.cpp`
- `Src/Libs/SimulatedNao/SimulatedRobot.h`

主要新增内容：

- 新命令：`kiba <x> <y> <speed>`
- `RobotConsole` 中增加 `kickBall()` 处理逻辑
- `SimulatedRobot` 中新增：
  - `setBallVelocity()`
  - `kickBallPerTeam()`

主要作用：

- 在仿真中直接把球踢向指定目标点
- 方便做踢球、传球、守门、反应测试

### 7.4 ConsoleRoboCupCtrl 集成

涉及文件：

- `Src/Libs/SimulatedNao/ConsoleRoboCupCtrl.h`
- `Src/Libs/SimulatedNao/ConsoleRoboCupCtrl.cpp`

主要修改：

- 在控制器中集成 `CommandServer`
- 新增 `gc external on|off` 命令
- 在 update 主循环中处理外部命令和外部 GC 消息

主要作用：

- 把上面的外部控制能力接入 SimRobot 主控制流程

### 7.5 新增 SimulatedNao11 占位目录

涉及内容：

- `Src/Libs/SimulatedNao11/SimulatedNao11.h`
- `Src/Libs/SimulatedNao11/SimulatedNao11.cpp`

主要作用：

- 预留 11 人制仿真相关扩展入口

---

## 8. SimulatedNao 编译兼容性修复

这是本次额外实际发生的代码修复，已经成功通过编译验证。

### 8.1 GameController 外部 GC 数据类型修复

涉及文件：

- `Src/Libs/SimulatedNao/GameController.cpp`

修复内容：

- 使用 `RoboCup::RoboCupGameControlData` 而非未带命名空间的类型名
- 使用基类引用赋值：
  - `static_cast<RoboCup::RoboCupGameControlData&>(gameControllerData) = *gcData;`

原因：

- `GameControllerData` 继承了 `RoboCup::RoboCupGameControlData` 和 `Streamable`
- 不能直接把基类对象赋给派生类对象
- 必须只拷贝其 `RoboCupGameControlData` 基类部分

### 8.2 消除 `recvfrom` / `recv` 的类型警告

涉及文件：

- `Src/Libs/SimulatedNao/GameController.cpp`
- `Src/Libs/SimulatedNao/CommandServer.cpp`

修复内容：

- `bytesRead` 由 `int` 改为 `ssize_t`
- `sizeof(...)` 比较时同步改为 `static_cast<ssize_t>(...)`

原因：

- Linux 下 `recv` / `recvfrom` 返回值为 `ssize_t`
- 原先使用 `int` 会触发精度缩窄 warning

效果：

- 编译通过且不再有该 warning

### 8.3 `setVelocity` API 适配修复

涉及文件：

- `Src/Libs/SimulatedNao/SimulatedRobot.cpp`

修复内容：

- 按当前 `SimRobotCore3::Body::setVelocity(const float* velocity)` 的接口形式调用
- 删除多余的第二个参数

原因：

- 当前 SimRobotCore3 API 只接受一个线速度参数指针
- 不能传入第二个角速度参数

效果：

- `kiba` 相关功能可顺利编译

---

## 9. 队伍与机器人配置

## 9.1 机器人配置目录

当前代码库中已经存在多台机器人配置目录，包括：

- `Config/Robots/fly1/`
- `Config/Robots/fly3/`
- `Config/Robots/fly4/`
- `Config/Robots/fly5/`
- `Config/Robots/fly6/`
- `Config/Robots/fly7/`
- `Config/Robots/fly8/`
- `Config/Robots/fly11/`
- `Config/Robots/fly12/`
- `Config/Robots/fly13/`
- `Config/Robots/fly14/`
- `Config/Robots/fly15/`
- `Config/Robots/fly16/`
- `Config/Robots/fly17/`
- `Config/Robots/fly18/`
- `Config/Robots/fly19/`
- `Config/Robots/fly20/`
- `Config/Robots/nb21/`
- `Config/Robots/nb22/`
- `Config/Robots/nb23/`

说明：

- 这些目录为实体机器人或测试机器人提供配置基础
- 部分目录可能是原先已有，部分可能是后续扩展时加入

## 9.2 `teams.cfg` 当前已落地配置

涉及文件：

- `Config/teams.cfg`

当前文件中已确认的配置如下：

### 第一支队伍

- 队名：`B-Human`
- 队号：`5`
- 场上颜色：`black`
- 守门员颜色：`black`
- 场景：`5vs5`
- 位置：`Default`
- 无线配置：`SPL_A`
- 音量：`63`
- magicNumber：`5`
- 当前已分配球员：`fly14`

### 第二支队伍

- 队名：`B-Team`
- 队号：`70`
- 场上颜色：`yellow`
- 守门员颜色：`yellow`
- 场景：`5vs5`
- 位置：`Default`
- 无线配置：`SPL_A`
- 音量：`62`
- magicNumber：`70`
- 当前已分配球员：`fly12`

主要作用：

- 为两支队伍分别提供本地部署配置
- 便于通过不同队号和球员映射进行部署与测试

注意：

- 当前 `name = B-Human;` / `name = B-Team;` 是未加引号写法，如果原版配置语法要求字符串带引号，后续部署时如遇解析问题，需要改为 `"B-Human"` / `"B-Team"`。

---

# 三、曾计划/曾改动，但当前已回退或未生效的内容

## 1. 第六点：开球策略修改

涉及文件：

- `Src/Modules/BehaviorControl/SkillBehaviorControl/Skills/Ball/DirectKickOff.cpp`

文档中原计划：

- 注释掉 `if(!theIndirectKick.allowDirectKick)` 相关的障碍扇区逻辑
- 让开球时不再限制正前方踢球方向

**当前实际状态**：

- **未生效**
- 当前代码仍然保留原版逻辑：
  - 当 `!theIndirectKick.allowDirectKick` 时，会在正前方加入障碍扇区

为什么当前保留原版是合理的：

- 按当前 2025/2026 SPL 规则，开球通常属于**间接开球**语义
- 直接大脚射门通常**不算进球**
- 如果去掉该限制，机器人可能更倾向于直接向前踢，收益不高，反而可能浪费开球机会

结论：

- 当前代码**没有应用该修改**
- 现阶段**保持原版更合理**

## 2. 第七点：己方半场传球限制

涉及文件：

- `Config/Scenarios/Default/optionPassToTeammate.cfg`
- `Config/Scenarios/Default/K1/optionPassToTeammate.cfg`
- `Config/Scenarios/Default/T1/optionPassToTeammate.cfg`
- `Src/Modules/BehaviorControl/SkillBehaviorControl/Skills/Ball/PassToTeammate.cpp`

文档中原计划：

- 在己方半场限制传球目标
- 只允许传给比球更靠近对方球门的队友
- 增加参数：
  - `restrictPassingInOwnHalf`
  - `minForwardDistanceInOwnHalf`

**当前实际状态**：

- **未生效 / 已回退**
- 当前 `PassToTeammate.cpp` 中没有该限制逻辑
- 当前三个 `optionPassToTeammate.cfg` 中也没有这两个限制参数
- 仅保留了 `lookAhead = true` 这类相对保守的优化

为什么当前未启用该修改：

- 5v5 可选传球目标本就较少
- 如果在己方半场强行限制“只能往前传”，容易导致**无合适传球对象**
- 该修改过于激进，风险大于收益

结论：

- 当前代码**没有应用该限制**
- 现阶段**不建议启用**

---

# 四、文档中提到，但当前未确认落地或当前未生效的内容

以下内容来自早期修改文档，但就当前代码状态来看，至少不是这次最终重点确认的“已落地核心项”。如有需要，后续可以逐项再次核对。

## 1. 日志回放修改

文档提及：

- `Config/Scenes/ReplayRobot.con`

文档中的目标：

- 自动执行 `gc standby -> gc ready -> gc set -> gc playing`
- 让日志回放自动进入 playing

当前说明：

- 该项在旧文档中有记录
- 但是否作为当前最终运行版本强依赖项，需要使用时再单独确认

## 2. Tactics 绘图增强

文档提及：

- `Config/Scenes/Drawings/Tactics.con`

文档中的目标：

- 增加守门员相关绘图，例如 `Goalkeeper:pose` 和 `Goalkeeper:coverage`

当前说明：

- 该项主要属于调试辅助，不影响比赛主逻辑

## 3. duck 声音注释

文档提及：

- `Src/Modules/BehaviorControl/PathPlannerProvider/PathPlannerProvider.cpp`

文档中的目标：

- 注释掉 `theValidityDuck.wav`

当前说明：

- 该项属于提示音层面的调整，不影响主功能

## 4. connectDialog 预设 IP

文档提及：

- `Config/Scenes/Includes/connectDialog.con`

当前说明：

- 该项已经存在预设 IP 内容，属于开发便利性增强

---

# 五、守门员相关说明

根据之前的核对结论，当前守门员主行为部分总体保持与原版一致。

包括以下方向：

- 没有继续保留早期 SPQR 风格的守门员速度威胁判定改动
- `Goalkeeper` 相关核心行为基本未做持续性改写

这意味着：

- 当前代码的守门员策略整体更接近官方原版
- 有利于减少高风险改动带来的不稳定性

---

# 六、本次代码整理后的实际结论

## 已明确落地且重要的内容

- 5v5 适配
- 开场站位调整
- 口哨检测优化
- 裁判手势识别优化
- 通信参数优化
- `lookAhead = true` 的传球改进
- SimRobot 外部控制扩展
- 外部 GameController3 支持
- `kiba` 命令
- `teams.cfg` 双队配置
- SimulatedNao 编译兼容修复

## 明确未落地 / 当前不建议启用的内容

- 开球时放开正前方限制
- 己方半场“只能往前传”的硬限制

## 当前策略取向

- 对比赛主行为的改动总体偏保守
- 对调试工具、仿真工具、外部接口的改动较积极
- 尽量避免高风险策略改动影响正式比赛稳定性

---

# 七、相关文件清单（按模块整理）

## 配置类

- `Config/settings.cfg`
- `Config/Scenarios/Default/strategyBehaviorControl.cfg`
- `Config/Scenarios/5vs5/setupPosesProvider.cfg`
- `Config/Scenarios/5vs5/strategyBehaviorControl.cfg`
- `Config/Scenarios/Default/setupPosesProvider.cfg`
- `Config/Scenarios/Default/gameStateProvider.cfg`
- `Config/Scenarios/Default/refereeGestureClassifier.cfg`
- `Config/Scenarios/Default/teamMessageHandler.cfg`
- `Config/Scenarios/Default/optionPassToTeammate.cfg`
- `Config/Scenarios/Default/K1/optionPassToTeammate.cfg`
- `Config/Scenarios/Default/T1/optionPassToTeammate.cfg`
- `Config/teams.cfg`

## 行为与基础模块

- `Src/Modules/Configuration/SetupPosesProvider/SetupPosesProvider.cpp`
- `Src/Modules/Infrastructure/GameStateProvider/GameStateProvider.cpp`
- `Src/Modules/BehaviorControl/SkillBehaviorControl/Options/HandleRefereeSignal.cpp`
- `Src/Modules/BehaviorControl/SkillBehaviorControl/Skills/Ball/DirectKickOff.cpp`
- `Src/Modules/BehaviorControl/SkillBehaviorControl/Skills/Ball/PassToTeammate.cpp`

## 仿真与工具扩展

- `Src/Libs/SimulatedNao/CommandServer.h`
- `Src/Libs/SimulatedNao/CommandServer.cpp`
- `Src/Libs/SimulatedNao/GameController.h`
- `Src/Libs/SimulatedNao/GameController.cpp`
- `Src/Libs/SimulatedNao/RobotConsole.h`
- `Src/Libs/SimulatedNao/RobotConsole.cpp`
- `Src/Libs/SimulatedNao/SimulatedRobot.h`
- `Src/Libs/SimulatedNao/SimulatedRobot.cpp`
- `Src/Libs/SimulatedNao/ConsoleRoboCupCtrl.h`
- `Src/Libs/SimulatedNao/ConsoleRoboCupCtrl.cpp`
- `Src/Libs/SimulatedNao11/SimulatedNao11.h`
- `Src/Libs/SimulatedNao11/SimulatedNao11.cpp`

## 场景与调试辅助

- `Config/Scenes/Game.con`
- `Config/Scenes/GameFast.con`
- `Config/Scenes/GamePerceptOracle.con`
- `Config/Scenes/Game.ros3`
- `Config/Scenes/GamePerceptOracle.ros3`
- `Config/Scenes/OtherScenes/OneTeamReferee.ros3`
- `Config/Scenes/Includes/5vs5.rsi3`
- `Config/Scenes/ReplayRobot.con`
- `Config/Scenes/Drawings/Tactics.con`
- `Config/Scenes/Includes/connectDialog.con`

---

# 八、建议

如果你后续要把这份文档作为正式项目记录，建议下一步再做两件事：

- 把每一项修改补上**具体参数前后对照表**
- 再补一份**部署说明**，记录如何使用 `teams.cfg`、`deploy`、外部 GC 和 `kiba`

这样会更方便后续交接、比赛前检查和版本回溯。

---

# 九、结语

这份文档描述的是**当前代码真正落地后的状态**，因此和早期的 `修改记录_完整版.md` 相比，更强调“现在代码里到底有哪些修改是有效的，哪些已经不做了”。

如果后续你还需要，我可以继续帮你再生成两份配套文档：

- `部署与使用说明.md`
- `相对官方原版的逐文件差异清单.md`
